spring.application.name=PFE
server.port=8080
# Profil local : charge application-local.properties (gitignored) pour SMTP, etc.
spring.profiles.active=local

# Si la base n'existe pas encore, le connecteur peut la  créer (compte MySQL avec droit CREATE).
spring.datasource.url=jdbc:mysql://localhost:3306/pfedbback?createDatabaseIfNotExist=true&characterEncoding=UTF-8&serverTimezone=Europe/Paris
spring.datasource.username=root
spring.datasource.password=
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Cloudinary (carnet numérique + photo profil élève)
app.cloudinary.cloud-name=drjcgspgc
app.cloudinary.api-key=326174511698576
app.cloudinary.api-secret=l2YqaVWQIioLHfUhuR9kw-OQ_No

# Comptes de connexion gérés côté backend (SOAP VerifierCompteBody)
educanet.security.admin-username=admin
educanet.security.admin-password=admin123
educanet.security.infirmier-username=infirmier
educanet.security.infirmier-password=infirmier123

# SMTP / email notifications
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=chadlianjoud@gmail.com
# Priorité : variable d'environnement SPRING_MAIL_PASSWORD, sinon application-local.properties
spring.mail.password=${SPRING_MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
management.health.mail.enabled=false
app.mail.from=${spring.mail.username}
app.mail.min-interval-ms=120000

# IA (Ollama) - recommandations non critiques
app.ai.ollama.enabled=true
app.ai.ollama.url=http://localhost:11434/api/generate
app.ai.ollama.model=llama3.1

